# Vibro 17 🤖

![Build APK](https://github.com/Miolonixc/Vibro17/actions/workflows/build.yml/badge.svg)

**Vibro 17** — Android-приложение в стиле Android 17 (Dragon Ball): тёмная тема,
электрический циан и набор вибро-эффектов для любых ситуаций.

> Нажми на карточку эффекта — вибромотор начнёт отыгрывать паттерн.
> Повторное нажатие или кнопка «Стоп» останавливают вибрацию.

## Эффекты

| Иконка | Эффект | Описание |
|--------|--------|----------|
| 🚂 | Поезд | Ритмичный перестук колёс |
| 🐦 | Птицы | Чириканье мелкими трелями |
| 🏎️ | Гоночная машинка | Ревущий мотор на старте |
| ❤️ | Сердцебиение | Пульс: тук-тук |
| 🚁 | Вертолёт | Гул несущего винта |
| 🔔 | Уведомление | Два коротких сигнала |
| 🚪 | Стук в дверь | Три резких удара |
| 🥁 | Барабан | Бит бас-барабана |
| 🆘 | SOS | Азбука Морзе ····———··· |
| 🪩 | Вечеринка | Стробоскопическая дискотека |
| 🔊 | Басы | Глубокие удары сабвуфера |
| 🛠️ | Дрель | Ровный гул электроинструмента |
| ☕ | Кофемолка | Дробное вибрирующее жужжание |
| 👽 | Инопланетянин | Загадочное переливающееся жужжание |
| 🌧️ | Дождь | Мягкие случайные капли |
| 💥 | Взрыв | Мощный удар с затуханием |
| 📱 | Ползти | Разгоняет телефон по столу |
| 🌙 | Сон | Убаюкивающее затухание вибрации |
| 🐝 | Пчела | Назойливое жужжание |
| 🚀 | Ракета | Взлёт с нарастающим гулом |
| 📞 | Звонок | Телефонный вызов |

Новые эффекты легко добавить в [`Effects.kt`](app/src/main/java/com/miolonixc/vibro17/model/Effects.kt).

## Анимация

Пока играет эффект:

- активная карточка пульсирует (масштаб + свечение рамки);
- в шапке мигает «живой» индикатор (`liveDot`).

Анимации реализованы через `res/anim/pulse.xml` и `res/anim/blink.xml`.

## Можно ли сдвинуть телефон вибрацией? 📱

**Да, частично.** Вибромотор большинства телефонов — это эксцентрик
(ERM, несбалансированный ротор): он создаёт неуравновешенную силу, и из-за
того, что мотор смещён относительно центра масс, телефон при работе
«приплясывает» и может медленно сползать по поверхности.

Чтобы это сработало заметнее:

1. Положи телефон **экраном вверх** на **гладкую, слегка скользкую**
   поверхность (стекло, полированный стол, пластик). На ковре/резиновом
   коврике не поедет.
2. Убери чехол — он гасит смещение.
3. Запусти эффект **«Ползти»** (непрерывная сильная вибрация) — он
   специально подобран, чтобы максимизировать «ползучесть».

Важно: это **неуправляемое** движение. Телефон не поедет в заданную
сторону — он будет описывать дуги/петли, зависящие от положения моторчика
и центра тяжести. Телефоны с линейным вибромотором (LRA, как в iPhone /
флагманах) ползут хуже, чем с классическим эксцентриком.

## Технические детали

- **Язык:** Kotlin
- **Минимальный SDK:** 21 (Android 5.0)
- **Целевой SDK:** 34
- **Амплитуда:** используется на API 26+ (`VibrationEffect.createWaveform`
  с массивом амплитуд). На старых устройствах вибрация работает без
  регулировки силы (грациозная деградация).
- **Разрешение:** `android.permission.VIBRATE` (не требует runtime-запроса).

### Архитектура

```
model/VibroEffect.kt   – data class эффекта (timings / amplitudes / repeat)
model/Effects.kt       – готовый набор пресетов
model/CustomStore.kt   – сохранение пользовательских эффектов в JSON
engine/VibrationEngine.kt – обёртка над Vibrator / VibratorManager
ui/MainActivity.kt     – экран с сеткой эффектов + запуск редактора
ui/EffectAdapter.kt    – RecyclerView-адаптер карточек
ui/CustomActivity.kt   – редактор собственных паттернов (ползунки)
```

## Редактор своих эффектов

Кнопка-карандаш (FAB слева внизу) открывает экран **Редактор**:

- введи название;
- добавляй **шаги** — каждый шаг задаётся двумя ползунками:
  - **Длительность** (10–600 мс) — сколько длится этот сегмент;
  - **Сила** (0–255) — амплитуда; `0` = пауза (вибрации нет);
- **▶ Предпросмотр** проигрывает паттерн сразу;
- **💾 Сохранить** добавляет эффект в сетку на главном экране.

Сохранённые эффекты пишутся во внутренний файл
`custom_effects.json` (`CustomStore.kt`, через `org.json`) и переживают
перезапуск приложения.

Пример паттерна «два удара в дверь» в редакторе:

| Шаг | Длительность | Сила |
|-----|--------------|------|
| 1   | 70 мс        | 255  |
| 2   | 45 мс        | 0    |
| 3   | 70 мс        | 255  |
| 4   | 260 мс       | 0    |

## Сборка

Открой проект в **Android Studio** (или собери через Gradle):

```bash
./gradlew assembleDebug      # отладочный APK (без подписи)
./gradlew assembleRelease    # релизный APK (нужен ключ — см. «Версии и релизы»)
```

APK появится в `app/build/outputs/apk/debug/` или `.../release/`.

## Установка

Перенеси `app-debug.apk` на устройство и установи (разреши установку из
неизвестных источников).

Подробности о версионировании, сборке и публикации релиза — в разделе
[«Версии и релизы»](#версии-и-релизы) ниже.

## Планы разработки (roadmap)

Более свежий статус по версиям — в разделе
[«Версии и релизы»](#версии-и-релизы) ниже. Кратко:

- [x] Удаление кастомных эффектов (долгое нажатие на карточку) — `0.1.0`
- [x] Экспорт / импорт паттернов (шаринг через JSON) — `0.2.0`
- [x] Категории и вкладки эффектов — `0.5.0`
- [x] Визуализатор в реальном времени, синхронный с вибрацией — `0.2.0`
- [x] Больше пресетов (тату-машинка, сирена, мурлыканье, массаж, пчела, ракета, звонок) — `0.1.0`/`0.5.0`
- [x] Плавное нарастание силы (amplitude ramp) в редакторе — `0.8.0`
- [x] Юнит-тесты на Kotlin (проверка длин массивов эффектов) — `0.8.1`

## Версии и релизы

Используется семантическое версионирование, заданное в
[`app/build.gradle`](app/build.gradle) (`versionNameValue`):

| Версия   | Значение |
|----------|----------|
| `0.0.x`  | патч / мелкие правки и багфиксы |
| `0.x.0`  | минорное обновление (новые эффекты, экраны) |
| `x.0.0`  | мажорное обновление (ломающие изменения, редизайн) |

`versionCode` вычисляется автоматически: `MAJOR*10000 + MINOR*100 + PATCH`
(так `0.0.2` → 2, `0.1.0` → 100, `1.0.0` → 10000).

**Подписанный релиз** собирается CI при пуше тега `vX.Y.Z`:

1. Локально сгенерируй ключ (один раз):

   ```bash
   keytool -genkeypair -v -keystore keys/vibro17-release.keystore \
     -alias vibro17 -keyalg RSA -keysize 2048 -validity 10000
   ```

2. Заполни `local.properties` (шаблон — `local.properties.example`, файл в
   `.gitignore`). Для CI те же значения кладутся в **GitHub Secrets**:
   `VIBRO17_KEYSTORE_BASE64`, `VIBRO17_KEYSTORE_PASSWORD`,
   `VIBRO17_KEY_ALIAS`, `VIBRO17_KEY_PASSWORD`.

3. Подними версию, закоммить, поставь тег и пуши:

   ```bash
   git tag v0.0.3 && git push origin v0.0.3
   ```

   Workflow [`.github/workflows/release.yml`](.github/workflows/release.yml)
   соберёт подписанный APK и создаст **GitHub Release** с артефактом.

## Планы разработки

Статус по версиям:

- ✅ **`0.1.0`** — 4 новых пресета (тату-машинка, сирена, мурлыканье, массаж);
  долгое нажатие по карточке кастомного эффекта → редактировать / удалить.
- ✅ **`0.2.0`** — **экспорт / импорт** кастомных паттернов файлом JSON
  (кнопки `EXP` / `IMP` на главном экране, системный выбор файла) и
  **визуализатор** амплитуды (анимированная волна над сеткой).
- ✅ **`0.3.0`** — **Quick Settings tile** (Android 7.0+) и **виджет** на главный
  экран с кнопками Поезд / Вечеринка / Стоп для быстрого запуска.
- ✅ **`0.4.0`** — **Избранное** (★ на карточке, сортировка избранных в начало),
  **глобальная сила** вибрации (ползунок 5–100%, сохраняется), сцена **«Сон»**
  (затухающая вибрация), экран **«О приложении»** с версией.
- ✅ **`0.5.0`** — **Фильтр по категориям** (чипы: Все / Транспорт / Природа /
  Техно / Сцены / Кастом) и ещё пресеты: 🐝 Пчела, 🚀 Ракета, 📞 Звонок
  (всего 25 эффектов).
- ✅ **`0.6.0`** — **Переключение тем** акцента: Android 17 (циан) / Классика
  (зелёный) / Янтарь. Кнопка 🎨 в шапке циклит тему, выбор сохраняется;
  акцент проброшен через `?attr/vibroAccent` по всему UI.
- ✅ **`0.7.0`** — **Сцены**: 🎉 «Вечеринка» (авто-перебор случайных эффектов
  в цикле) и 🌙 «Сон» (выбранный эффект играет в цикле и плавно затихает за
  5/15/30/60 мин, счётчик обратного отсчёта в статус-строке).
- ✅ **`0.7.1`** — **Tile запускает избранный эффект**: плитка Quick Settings теперь
  проигрывает первый отмеченный ★ эффект (или дефолтный), а в подписи показывает
  его название. Долгий тап в шторке → «Vibro 17».
- ✅ **`0.8.0`** — **Амплитудный ramp в редакторе**: переключатель «Плавное
  нарастание и затухание» накладывает треугольную огибающую на силу шагов
  (нарастание в первой трети, удержание, спад в последней трети) при
  предпросмотре и сохранении.
- ✅ **`0.8.1`** — **Юнит-тесты** на пресеты (`EffectsTest`): инварианты длин
  массивов, диапазон амплитуд, неотрицательные длительности, уникальность id.
  Прогоняются в CI (`build.yml`) на каждом пуше.

### Быстрый запуск (tile + виджет)

- **Плитка** добавляется в шторку Quick Settings вручную (карандаш →
  «Vibro 17»). Тап = вкл/выкл **первого избранного (★)** эффекта; в подписи
  видно его название.
- **Виджет** добавляется как обычный виджет на рабочий стол; кнопки
  🚂 / 🪩 / ⏹ проигрывают эффект или останавливают вибрацию.

Оба компонента используют тот же `VibrationEngine` и набор `Effects.ALL`.

### Экспорт / импорт

- `EXP` сохраняет все кастомные эффекты в JSON-файл (через системный
  picker — `ActionResultContracts.CreateDocument`).
- `IMP` читает ранее сохранённый файл и добавляет/обновляет эффекты
  (дубликаты по `id` перезаписываются).

Формат файла — массив объектов `{id,title,subtitle,icon,timings,amplitudes,repeat}`,
тот же, что и во внутреннем `custom_effects.json` (`CustomStore.kt`).

## Лицензия

MIT — см. [LICENSE](LICENSE).
